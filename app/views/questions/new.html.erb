<h1>New Questions</h1>

<%= form_for @q do |f| %>
   <%= f.label :title %>
   <%= f.text_field :title %>
   <%= f.label :body %>
   <%= f.text_area :body %>
   <%= f.label :resolved %>
   <%= f.check_box :resolved %>
   <%= f.submit "Save" %>
 <% end %>